export interface VehicleInformation {
  /**
   * Brand code of the vehicle
   *
   * @type {number}
   * @memberof VehicleInformation
   */
  brand: number;

  year: number;

  model: string;

  version?: string;
}
