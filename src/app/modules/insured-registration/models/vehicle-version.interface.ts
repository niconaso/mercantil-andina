export interface VehicleVersion {
  /**
   * Version description
   *
   * @type {string}
   * @memberof VehicleVersion
   */
  desc: string;

  /**
   * Internal code
   *
   * @type {number}
   * @memberof VehicleVersion
   */
  codigo: number;
}
